<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.log450.bchainvoteversion1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BChainVoteVersion1">
        <activity android:name=".UI.Activities.BlockchainActivity"></activity>
        <activity android:name=".UI.Activities.CandidateActivity" />
        <activity android:name=".UI.Activities.ElectoralActivity" />
        <activity android:name=".UI.Activities.ElectoralAuthentication" />
        <activity android:name=".UI.Activities.CredentialsActivity" />
        <activity android:name=".UI.Activities.Menu" />
        <activity android:name=".UI.Activities.CastVote" />
        <activity android:name=".UI.Activities.Vote" />
        <activity android:name=".UI.Activities.Login" />
        <activity android:name=".UI.Activities.Register" />
        <activity android:name=".UI.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>